language: node_js

node_js:
  - "8"

env:
  matrix:
    - TEST_TYPE=lint
    - TEST_TYPE=unit-test
    - TEST_TYPE=e2e

script:
  - |
    if [ "$TEST_TYPE" = lint ]; then
      npm run lint
    elif [ "$TEST_TYPE" = uitest ]; then
      npm run unit-test
    elif [ "$TEST_TYPE" = e2e ]; then
      npm run site
      mv dist/* ./
      ls
      php -S localhost:8000 &
      npm run e2e
    fi
